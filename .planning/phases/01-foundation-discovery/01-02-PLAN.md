---
phase: 01-foundation-discovery
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - custom_components/meraki_ha/__init__.py
  - custom_components/meraki_ha/discovery/coordinator.py
  - custom_components/meraki_ha/discovery/service.py
  - tests/discovery/test_coordinator.py
autonomous: true
requirements: [FND-02, FND-03]

must_haves:
  truths:
    - 'Setup entry logic in __init__.py is modularized and clean'
    - 'Organizations and Networks are represented as service devices in HA'
    - 'A hierarchical relationship exists (Org is parent of Network)'
    - 'Hardware devices are discovered and mapped to their respective networks'
  artifacts:
    - path: 'custom_components/meraki_ha/discovery/coordinator.py'
      provides: 'Hierarchical discovery for Meraki Organizations and Networks'
    - path: 'custom_components/meraki_ha/__init__.py'
      provides: 'Async integration entry setup and lifecycle management'
  key_links:
    - from: 'custom_components/meraki_ha/__init__.py'
      to: 'custom_components/meraki_ha/discovery/coordinator.py'
      via: 'Initialization during async_setup_entry'
    - from: 'custom_components/meraki_ha/discovery/coordinator.py'
      to: 'homeassistant.helpers.device_registry'
      via: 'Creating hierarchical device entries'
---

<objective>
Refactor the integration entry lifecycle for modularity and implement robust hierarchical discovery of Meraki cloud objects to Home Assistant devices.

Purpose: Improve codebase maintainability and establish a clean representation of the Meraki infrastructure in the Home Assistant Device Registry.
Output: Refactored setup logic and functional hierarchical device mapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@custom_components/meraki_ha/__init__.py
@custom_components/meraki_ha/discovery/coordinator.py
@custom_components/meraki_ha/discovery/service.py
@tests/discovery/test_coordinator.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor async_setup_entry in __init__.py</name>
  <files>custom_components/meraki_ha/__init__.py</files>
  <behavior>
    -Setup steps (Client factory, Coordinators, Services, Webhooks, Discovery) are extracted into helper methods.
    -Error handling during setup is isolated per module to avoid total failure on optional components (like webhooks).
    -Integration setup does not perform blocking I/O on the event loop.
  </behavior>
  <action>
    1. Identify logical sections in __init__.py's async_setup_entry.
    2. Extract client initialization, coordinator setup, service registration, and webhook registration into separate async methods.
    3. Ensure each step is awaited and correctly populates hass.data.
    4. Implement robust try/except blocks around optional setup steps.
  </action>
  <verify>
    <automated>pytest tests/test_meraki_client_init.py -x</automated>
  </verify>
  <done>async_setup_entry is refactored into a modular, testable, and maintainable structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify and Harden Hierarchical Discovery</name>
  <files>custom_components/meraki_ha/discovery/coordinator.py, custom_components/meraki_ha/discovery/service.py, tests/discovery/test_coordinator.py</files>
  <behavior>
    - Meraki Organizations are created as devices with 'SERVICE' entry type.
    - Meraki Networks are created as devices and linked to their parent Org via via_device.
    - Hardware devices discovered in Phase 1 use the network device as parent where applicable.
  </behavior>
  <action>
    1. Review discovery/coordinator.py to ensure _async_update_device_registry correctly links Orgs and Networks.
    2. Confirm DeviceDiscoveryService correctly processes the Meraki cloud hierarchy and populates registries.
    3. Add or update tests in tests/discovery/test_coordinator.py to verify device linking (identifiers and via_device).
  </action>
  <verify>
    <automated>pytest tests/discovery/test_coordinator.py -x</automated>
  </verify>
  <done>Meraki Organizations, Networks, and Devices are correctly mapped and linked in the HA Device Registry.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                                         |
| --------------------------------- | --------------------------------------------------- |
| Integration -> HA Device Registry | Internal HA API call to modify the device database. |

## STRIDE Threat Register

| Threat ID  | Category               | Component        | Disposition | Mitigation Plan                                                                                                                 |
| ---------- | ---------------------- | ---------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------- |
| T-01-02-01 | Information Disclosure | Metadata storage | mitigate    | Only non-sensitive metadata (names, IDs, models) is stored in the device registry. Serial numbers are used as identifiers only. |
| T-01-02-02 | Integrity              | Device Mapping   | mitigate    | Ensure identifiers are unique and use the (DOMAIN, identifier) format to prevent collision with other integrations.             |

</threat_model>

<verification>
Execute integration tests to ensure that upon setup, the HA device registry contains the expected Meraki organization and network devices, and that the networks are properly parented to the organization.
</verification>

<success_criteria>

- [ ] async_setup_entry is modular and easily readable.
- [ ] Organization device exists in HA.
- [ ] Network devices exist and are linked to the Organization device.
- [ ] Hardware devices are correctly parented to their respective Networks.
- [ ] Setup completes without blocking the Home Assistant event loop.
      </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-02-SUMMARY.md`
</output>
