---
phase: 01-foundation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/meraki_ha/config_flow.py
  - custom_components/meraki_ha/reauth_flow.py
  - custom_components/meraki_ha/schemas.py
  - tests/test_config_flow.py
autonomous: true
requirements: [FND-01]

must_haves:
  truths:
    - 'User enters API Key in first step and it is validated via an API call'
    - 'Organizations for the API Key are fetched and presented in a selection step'
    - 'Networks for the selected Organization are fetched and presented in a selection step'
    - 'Config flow handles invalid API keys and connection errors with user-friendly messages'
  artifacts:
    - path: 'custom_components/meraki_ha/config_flow.py'
      provides: 'Multi-step async configuration flow'
    - path: 'custom_components/meraki_ha/reauth_flow.py'
      provides: 'Reauthentication logic for expired/rotated API keys'
  key_links:
    - from: 'custom_components/meraki_ha/config_flow.py'
      to: 'custom_components/meraki_ha/core/api/factory.py'
      via: 'API client creation for validation'
---

<objective>
Harden and verify the multi-step async configuration flow to ensure secure connectivity and hierarchical selection of Meraki Organizations and Networks.

Purpose: Provide a robust initial setup experience that validates credentials and allows users to selective monitor their Meraki infrastructure.
Output: Verified, error-resilient config flow and reauth flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@custom_components/meraki_ha/config_flow.py
@custom_components/meraki_ha/reauth_flow.py
@custom_components/meraki_ha/schemas.py
@tests/test_config_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden Config Flow Error Handling & Validation</name>
  <files>custom_components/meraki_ha/config_flow.py, custom_components/meraki_ha/schemas.py</files>
  <behavior>
    - User enters invalid API key -> show 'invalid_auth' error.
    - User enters API key with no organizations -> show 'no_orgs' error.
    - Connection to Meraki API fails -> show 'cannot_connect' error.
    - All API calls in config flow are properly awaited and async-safe.
  </behavior>
  <action>
    1. Review and refine async_step_user, async_step_org, and async_step_networks in config_flow.py.
    2. Ensure create_api_client is used correctly with proper async_setup call before making API requests.
    3. Update schemas.py if needed to ensure validation rules match the expected input types.
    4. Implement exhaustive error catching for MerakiAuthenticationError, MerakiConnectionError, and generic exceptions.
  </action>
  <verify>
    <automated>pytest tests/test_config_flow.py -k "test_user_step" -x</automated>
  </verify>
  <done>Config flow is robust against common API errors and correctly chains multi-step selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Robust Reauth Flow Integration</name>
  <files>custom_components/meraki_ha/reauth_flow.py, tests/test_reauth_flow.py</files>
  <behavior>
    - Existing entry triggered for reauth -> prompt for API key.
    - Updated credentials validated before updating config entry.
    - Integration reloaded after successful reauthentication.
  </behavior>
  <action>
    1. Verify async_step_reauth in reauth_flow.py correctly extracts the existing entry_id from context.
    2. Ensure validate_meraki_credentials is used for verification before updating entry data.
    3. Confirm the config entry is reloaded using hass.config_entries.async_reload.
    4. Add a test case to tests/test_reauth_flow.py for a successful reauth cycle.
  </action>
  <verify>
    <automated>pytest tests/test_reauth_flow.py -x</automated>
  </verify>
  <done>Reauthentication flow is fully functional and safely updates existing integration entries.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary      | Description                                                              |
| ------------- | ------------------------------------------------------------------------ |
| Client -> API | Sensitive API keys cross this boundary; must be handled over HTTPS only. |

## STRIDE Threat Register

| Threat ID  | Category               | Component          | Disposition | Mitigation Plan                                                                                              |
| ---------- | ---------------------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------ |
| T-01-01-01 | Information Disclosure | Config Flow Logs   | mitigate    | Ensure API keys are never logged by using SDK's suppress_logging=True (already implemented in MerakiClient). |
| T-01-01-02 | Spoofing               | API Authentication | mitigate    | Mandatory validation of API key and Organization ID during setup/reauth.                                     |

</threat_model>

<verification>
Run the config flow manually in Home Assistant or execute the comprehensive test suite to confirm all steps (User -> Org -> Networks) work as intended.
</verification>

<success_criteria>

- [ ] User can successfully configure the integration via the UI.
- [ ] Multi-step selection (Org -> Network) works correctly.
- [ ] Reauthentication updates the existing entry without creating duplicates.
- [ ] API keys are validated before being stored.
      </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-01-SUMMARY.md`
</output>
