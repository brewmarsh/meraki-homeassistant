---
phase: 01-foundation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/meraki_ha/core/api/client.py
  - custom_components/meraki_ha/core/api/endpoints/appliance.py
  - custom_components/meraki_ha/core/api/endpoints/camera.py
  - custom_components/meraki_ha/core/api/endpoints/devices.py
  - custom_components/meraki_ha/core/api/endpoints/network.py
  - custom_components/meraki_ha/core/api/endpoints/organization.py
  - custom_components/meraki_ha/core/api/endpoints/sensor.py
  - custom_components/meraki_ha/core/api/endpoints/switch.py
  - custom_components/meraki_ha/core/api/endpoints/wireless.py
  - custom_components/meraki_ha/config_flow.py
  - custom_components/meraki_ha/schemas.py
autonomous: true
requirements: [FND-01, FND-02]

must_haves:
  truths:
    - 'MerakiClient uses meraki.aio.AsyncDashboardAPI for all network I/O'
    - 'Config flow presents API Key, then Org Selection, then Network Selection'
    - 'API Key validation occurs before Org selection'
  artifacts:
    - path: 'custom_components/meraki_ha/core/api/client.py'
      provides: 'Async Meraki API client'
    - path: 'custom_components/meraki_ha/config_flow.py'
      provides: 'Multi-step configuration flow'
  key_links:
    - from: 'custom_components/meraki_ha/config_flow.py'
      to: 'custom_components/meraki_ha/core/api/client.py'
      via: 'API calls for Orgs/Networks'
---

<objective>
Refactor the core API client to use native async (meraki.aio) and implement a multi-step configuration flow for selective Organization and Network discovery.

Purpose: Improve performance by removing threaded executor overhead and allow users to select specific networks to monitor.
Output: Modernized API client and hierarchical config flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@custom_components/meraki_ha/core/api/client.py
@custom_components/meraki_ha/config_flow.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor MerakiClient for meraki.aio</name>
  <files>
    custom_components/meraki_ha/core/api/client.py,
    custom_components/meraki_ha/core/api/endpoints/appliance.py,
    custom_components/meraki_ha/core/api/endpoints/camera.py,
    custom_components/meraki_ha/core/api/endpoints/devices.py,
    custom_components/meraki_ha/core/api/endpoints/network.py,
    custom_components/meraki_ha/core/api/endpoints/organization.py,
    custom_components/meraki_ha/core/api/endpoints/sensor.py,
    custom_components/meraki_ha/core/api/endpoints/switch.py,
    custom_components/meraki_ha/core/api/endpoints/wireless.py
  </files>
  <behavior>
    - MerakiClient.async_setup() initializes meraki.aio.AsyncDashboardAPI with HA clientsession.
    - MerakiClient.run_async() replaces run_sync() and directly awaits SDK coroutines.
    - Endpoint handlers use run_async() and pass coroutine functions.
  </behavior>
  <action>
    1. In client.py, import meraki.aio and replace DashboardAPI with AsyncDashboardAPI.
    2. Update async_setup to pass aiohttp_session=async_get_clientsession(self._hass).
    3. Rename run_sync to run_async and refactor to await the provided func(*args, **kwargs).
    4. Remove usage of loop.run_in_executor and async_add_executor_job for API calls.
    5. Update all 8 endpoint handlers to call run_async and handle awaitables.
  </action>
  <verify>
    <automated>pytest tests/test_meraki_client_init.py -x</automated>
  </verify>
  <done>MerakiClient and all endpoint handlers are fully asynchronous using meraki.aio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement multi-step Config Flow</name>
  <files>custom_components/meraki_ha/config_flow.py, custom_components/meraki_ha/schemas.py</files>
  <behavior>
    - User enters API Key in async_step_user.
    - async_step_org fetches and displays Organizations for selection.
    - async_step_networks fetches and displays Networks for the selected Org (multi-select).
  </behavior>
  <action>
    1. Update async_step_user to only prompt for API Key and validate connectivity.
    2. Implement async_step_org: fetch Orgs using MerakiClient, show list to user.
    3. Implement async_step_networks: fetch Networks for selected Org, show multi-select list.
    4. Update schemas.py to include placeholders for dynamic Org/Network lists.
    5. Store selected Org ID and Network IDs in the config entry data.
  </action>
  <verify>
    <automated>pytest tests/test_config_flow.py -x</automated>
  </verify>
  <done>Configuration flow supports Organization and Network selection steps.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                             |
| ------------------ | --------------------------------------- |
| HA -> Meraki Cloud | Encrypted API communication over HTTPS. |

## STRIDE Threat Register

| Threat ID | Category               | Component    | Disposition | Mitigation Plan                                                    |
| --------- | ---------------------- | ------------ | ----------- | ------------------------------------------------------------------ |
| T-01-01   | Spoofing               | config_flow  | mitigate    | Validate API key by fetching Orgs during the first step.           |
| T-01-02   | Information Disclosure | logging      | mitigate    | Use SDK's suppress_logging=True to avoid leaking API keys in logs. |
| T-01-03   | Denial of Service      | MerakiClient | mitigate    | Maintain 2-request semaphore and wait_on_rate_limit=True.          |

</threat_model>

<verification>
Run `pytest tests/test_config_flow.py` and `tests/test_meraki_client_init.py` to ensure the new flow and client logic are sound.
</verification>

<success_criteria>

1. MerakiClient successfully initializes using meraki.aio.
2. Config flow correctly chains API Key -> Org -> Networks steps.
3. Config entry data contains the validated API key, Org ID, and list of enabled Network IDs.
   </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-01-SUMMARY.md`
</output>
