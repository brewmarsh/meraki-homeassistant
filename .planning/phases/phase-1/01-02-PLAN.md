---
phase: 01-foundation-discovery
plan: 02
type: execute
wave: 2
depends_on: ['01-01']
files_modified:
  - custom_components/meraki_ha/__init__.py
  - custom_components/meraki_ha/discovery/coordinator.py
autonomous: true
requirements: [FND-02, FND-03]

must_haves:
  truths:
    - 'Integration only initializes coordinators for selected networks'
    - 'Organizations and Networks appear as Devices in the HA Device Registry'
    - 'Network Devices are linked to their parent Organization Device'
  artifacts:
    - path: 'custom_components/meraki_ha/discovery/coordinator.py'
      provides: 'Hierarchical Discovery Coordinator'
  key_links:
    - from: 'custom_components/meraki_ha/__init__.py'
      to: 'custom_components/meraki_ha/discovery/coordinator.py'
      via: 'Initialization during async_setup_entry'
---

<objective>
Update the integration lifecycle to handle selective network configuration and implement the Discovery Coordinator for mapping the Meraki hierarchy to Home Assistant devices.

Purpose: Optimize resource usage by only tracking user-selected networks and establish a clean hierarchical device structure in HA.
Output: Network-aware integration setup and hierarchical device mapping.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@custom_components/meraki_ha/__init__.py
@custom_components/meraki_ha/discovery/service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update __init__.py for selective network setup</name>
  <files>custom_components/meraki_ha/__init__.py</files>
  <behavior>
    - Extract enabled_networks from config entry.
    - Pass the list of enabled networks to all coordinators.
    - Ensure coordinators only fetch data for the configured networks.
  </behavior>
  <action>
    1. Update async_setup_entry to extract CONF_ENABLED_NETWORKS from entry.data/options.
    2. Store the enabled networks list in the api_client or a shared context.
    3. Modify coordinator initializations to receive and respect the enabled networks list.
    4. Ensure the Discovery Service only processes the selected networks.
  </action>
  <verify>
    <automated>pytest tests/test_meraki_client_init.py -x</automated>
  </verify>
  <done>Integration only initializes for and tracks the user-selected networks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Discovery Coordinator for Orgs/Networks</name>
  <files>custom_components/meraki_ha/discovery/coordinator.py</files>
  <behavior>
    - DiscoveryCoordinator fetches Organization and Network details.
    - Creates a Device in HA for the Organization.
    - Creates a Device in HA for each enabled Network, using the Org Device as parent (via_device).
  </behavior>
  <action>
    1. Create DiscoveryCoordinator inheriting from DataUpdateCoordinator.
    2. Implement _async_update_data to fetch current Org and selected Networks info.
    3. Use device_registry to create/update Devices for the Organization and Networks.
    4. Ensure Network Devices have the Organization Device serial/ID as via_device.
    5. Wire this coordinator into async_setup_entry in __init__.py.
  </action>
  <verify>
    <automated>pytest tests/test_topology.py -x</automated>
  </verify>
  <done>Organizations and Networks are correctly mapped to hierarchical HA devices.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                          | Description                           |
| --------------------------------- | ------------------------------------- |
| Integration -> HA Device Registry | Internal HA API for managing devices. |

## STRIDE Threat Register

| Threat ID | Category               | Component | Disposition | Mitigation Plan                                                                                |
| --------- | ---------------------- | --------- | ----------- | ---------------------------------------------------------------------------------------------- |
| T-01-04   | Information Disclosure | Registry  | mitigate    | Only store non-sensitive metadata (names, IDs, versions) in device registry.                   |
| T-01-05   | Denial of Service      | HA Core   | mitigate    | Run discovery coordinator with a reasonable scan_interval (e.g., 1 hour) or trigger on-demand. |

</threat_model>

<verification>
Check HA Device Registry to confirm "Cisco Meraki [Org Name]" and "Meraki Network [Network Name]" devices exist and are properly linked.
</verification>

<success_criteria>

1. Integration successfully loads only for selected networks.
2. Organization appears as a top-level device in HA.
3. Enabled Networks appear as devices linked to the Organization device.
   </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-discovery/01-02-SUMMARY.md`
</output>
