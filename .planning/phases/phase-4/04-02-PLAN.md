---
phase: 04-advanced-control-selective-stats
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/meraki_ha/sensor/uplink_performance.py
  - custom_components/meraki_ha/sensor/device/bandwidth.py
  - custom_components/meraki_ha/core/api/endpoints/appliance/uplink.py
autonomous: true
requirements:
  - CTRL-04
  - CTRL-05

must_haves:
  truths:
    - 'User can see real-time latency and packet loss for appliance uplinks.'
    - 'User can see bandwidth/throughput stats as opt-in sensors.'
  artifacts:
    - path: 'custom_components/meraki_ha/sensor/uplink_performance.py'
      provides: 'High-resolution performance telemetry'
    - path: 'custom_components/meraki_ha/sensor/device/bandwidth.py'
      provides: 'Uplink bandwidth usage history'
  key_links:
    - from: 'custom_components/meraki_ha/sensor/uplink_performance.py'
      to: 'MerakiMainCoordinator'
      via: 'performance data update'
---

<objective>
Implement high-resolution latency, packet loss, and opt-in bandwidth/throughput sensors.
- Refactor existing performance sensors to use specific history endpoints for reliability.
- Create new sensors for bandwidth usage over a 60s timespan.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-RESEARCH.md

<interfaces>
From custom_components/meraki_ha/core/api/endpoints/appliance/uplink.py:
```python
async def get_network_appliance_uplinks_performance(self, network_id: str) -> list[dict[str, Any]]:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Latency & Packet Loss sensors for Network Uplinks</name>
  <files>custom_components/meraki_ha/core/api/endpoints/appliance/uplink.py, custom_components/meraki_ha/sensor/uplink_performance.py</files>
  <action>
    - Refactor `get_network_appliance_uplinks_performance` to use `getNetworkApplianceUplinksLossAndLatency` when available in the SDK.
    - Update `MerakiUplinkPerformanceSensor` to handle the specific data format of `timeseries` if necessary.
    - Reference CTRL-05.
  </action>
  <verify>
    <automated>pytest tests/sensor/test_uplink_performance.py</automated>
  </verify>
  <done>Performance sensors provide accurate latency/loss data from Meraki history endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Opt-in Bandwidth & Throughput sensors</name>
  <files>custom_components/meraki_ha/sensor/device/bandwidth.py</files>
  <action>
    - Create `MerakiBandwidthSensor` that uses data from `getNetworkApplianceUplinksUsageHistory`.
    - Map 'sent' and 'recv' values to Megabytes per second or similar.
    - Ensure these sensors are disabled by default (opt-in) to prevent database bloat.
    - Reference CTRL-04.
  </action>
  <verify>
    <automated>pytest tests/sensor/device/test_bandwidth.py</automated>
  </verify>
  <done>Bandwidth usage sensors are available and provide throughput metrics.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                 |
| ------------------------- | --------------------------- |
| Integration -> Meraki API | Telemetry data crosses here |

## STRIDE Threat Register

| Threat ID | Category               | Component        | Disposition | Mitigation Plan                          |
| --------- | ---------------------- | ---------------- | ----------- | ---------------------------------------- |
| T-04-03   | Information Disclosure | Bandwidth Sensor | accept      | Usage data is non-PII and internal to HA |

</threat_model>

<verification>
1. Run `pytest tests/sensor/test_uplink_performance.py`
2. Run `pytest tests/sensor/device/test_bandwidth.py`
</verification>

<success_criteria>

- Uplink metrics sensors (latency, packet loss) are working.
- Bandwidth usage sensors are implemented as opt-in.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-4/04-02-SUMMARY.md`
</output>
