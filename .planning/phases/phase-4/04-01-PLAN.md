---
phase: 04-advanced-control-selective-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/meraki_ha/switch/meraki_client_blocker.py
  - custom_components/meraki_ha/core/coordinators/ssid_firewall_coordinator.py
  - custom_components/meraki_ha/switch/meraki_ssid_device_switch.py
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03

must_haves:
  truths:
    - 'User can block/unblock clients using MAC-based policy.'
    - 'SSID toggles work reliably using the updateNetworkSsid API.'
  artifacts:
    - path: 'custom_components/meraki_ha/core/coordinators/ssid_firewall_coordinator.py'
      provides: 'Client blocking logic'
    - path: 'custom_components/meraki_ha/switch/meraki_client_blocker.py'
      provides: 'Client blocker switch entity'
  key_links:
    - from: 'custom_components/meraki_ha/switch/meraki_client_blocker.py'
      to: 'SsidFirewallCoordinator'
      via: 'coordinator update/calls'
---

<objective>
Refactor SSID toggles and client blocking to use reliable Meraki APIs.
- Refactor client blocking from IP-based firewall rules to MAC-based Client Policy API.
- Refactor SSID toggles to use the standard Meraki SSID update API reliably.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-RESEARCH.md

<interfaces>
From custom_components/meraki_ha/core/api/protocol.py:
```python
async def update_network_client_policy(self, network_id: str, client_mac: str, device_policy: str) -> dict[str, Any]:
    ...
```

From custom_components/meraki_ha/core/api/endpoints/wireless.py:

```python
async def update_network_wireless_ssid(self, network_id: str, number: int, **kwargs: Any) -> dict[str, Any]:
    ...
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MAC-based Client Policy in SsidFirewallCoordinator</name>
  <files>custom_components/meraki_ha/core/coordinators/ssid_firewall_coordinator.py</files>
  <action>
    - Complete the `SsidFirewallCoordinator` class.
    - Implement `async_block_client(client_mac: str, network_id: str)` and `async_unblock_client(client_mac: str, network_id: str)`.
    - Use `update_network_client_policy(network_id, client_mac, device_policy='Blocked')` for blocking.
    - Use `update_network_client_policy(network_id, client_mac, device_policy='Normal')` for unblocking.
    - Ensure it handles errors via the Meraki API client.
  </action>
  <verify>
    <automated>pytest tests/switch/test_meraki_client_blocker.py</automated>
  </verify>
  <done>Coordinator implements MAC-based blocking/unblocking methods.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MerakiClientBlockerSwitch to use MAC-based policy</name>
  <files>custom_components/meraki_ha/switch/meraki_client_blocker.py</files>
  <action>
    - Update `MerakiClientBlockerSwitch` to use `_client_mac` instead of `_client_ip` for blocking calls.
    - Update `_update_internal_state` to check for client policy status from the coordinator (if available) or directly from client data.
    - Reference CTRL-02.
  </action>
  <verify>
    <automated>pytest tests/switch/test_meraki_client_blocker.py</automated>
  </verify>
  <done>Client blocker switch uses MAC-based policy API calls.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor SSID toggles for reliability</name>
  <files>custom_components/meraki_ha/switch/meraki_ssid_device_switch.py</files>
  <action>
    - Modify `MerakiSSIDBaseSwitch._update_ssid_setting`.
    - Await the `update_network_wireless_ssid` call instead of using `async_create_task` or ensure it's properly tracked in the coordinator's pending updates.
    - Ensure it uses the network ID and SSID number correctly from the data.
    - Reference CTRL-01.
  </action>
  <verify>
    <automated>pytest tests/switch/test_meraki_ssid_device_switch.py</automated>
  </verify>
  <done>SSID toggles are reliable and await API confirmation.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                 |
| ------------------------- | --------------------------- |
| Integration -> Meraki API | Control commands cross here |

## STRIDE Threat Register

| Threat ID | Category          | Component     | Disposition | Mitigation Plan                                    |
| --------- | ----------------- | ------------- | ----------- | -------------------------------------------------- |
| T-04-01   | Tampering         | SSID Switch   | mitigate    | Use session-scoped API calls with token validation |
| T-04-02   | Denial of Service | Client Policy | mitigate    | Apply rate limiting to control commands            |

</threat_model>

<verification>
1. Run `pytest tests/switch/test_meraki_client_blocker.py`
2. Run `pytest tests/switch/test_meraki_ssid_device_switch.py`
</verification>

<success_criteria>

- Client blocking works via MAC policy.
- SSID toggles successfully update Meraki state.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-4/04-01-SUMMARY.md`
</output>
