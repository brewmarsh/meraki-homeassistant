---
phase: 04-advanced-control-selective-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/meraki_ha/switch/meraki_client_blocker.py
  - custom_components/meraki_ha/core/coordinators/ssid_firewall_coordinator.py
  - custom_components/meraki_ha/switch/meraki_ssid_device_switch.py
autonomous: true
requirements:
  - CTRL-01
  - CTRL-02
  - CTRL-03

must_haves:
  truths:
    - 'User can block/unblock clients using MAC-based policy.'
    - 'SSID toggles work reliably using the updateNetworkSsid API.'
  artifacts:
    - path: 'custom_components/meraki_ha/core/coordinators/ssid_firewall_coordinator.py'
      provides: 'Client blocking logic and state fetcher'
    - path: 'custom_components/meraki_ha/switch/meraki_client_blocker.py'
      provides: 'Client blocker switch entity with real-time state'
  key_links:
    - from: 'custom_components/meraki_ha/switch/meraki_client_blocker.py'
      to: 'SsidFirewallCoordinator'
      via: 'coordinator update/calls'
---

<objective>
Refactor SSID toggles and client blocking to use reliable Meraki APIs and ensure accurate state management.
- Update SsidFirewallCoordinator to fetch and update client policy states.
- Refactor MerakiClientBlockerSwitch to reflect the real-time policy status.
- Harden SSID toggles with better error handling and state verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-RESEARCH.md

<interfaces>
From custom_components/meraki_ha/core/api/protocol.py:
```python
async def update_network_client_policy(self, network_id: str, client_mac: str, device_policy: str) -> dict[str, Any]:
    ...
```

From custom_components/meraki_ha/core/api/endpoints/wireless.py:

```python
async def update_network_wireless_ssid(self, network_id: str, number: int, **kwargs: Any) -> dict[str, Any]:
    ...
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete SsidFirewallCoordinator with State Fetching</name>
  <files>custom_components/meraki_ha/core/coordinators/ssid_firewall_coordinator.py</files>
  <action>
    - Update `_async_update_data` to fetch current client policies for active clients using `get_network_clients`.
    - Ensure `async_block_client` and `async_unblock_client` correctly update the internal state and trigger refreshes.
    - Implement a mapping for MAC to policy status to be used by switches.
  </action>
  <verify>
    <automated>pytest tests/switch/test_meraki_client_blocker.py</automated>
  </verify>
  <done>Coordinator fetches and stores MAC-based policy statuses.</done>
</task>

<task type="auto">
  <name>Task 2: Harden MerakiClientBlockerSwitch for Accurate State</name>
  <files>custom_components/meraki_ha/switch/meraki_client_blocker.py</files>
  <action>
    - Update `_update_internal_state` to strictly use the coordinator's fetched policy data.
    - Add logic to handle cases where a client's policy is not yet known.
    - Ensure `async_turn_on`/`async_turn_off` provides immediate UI feedback with reversal on failure.
  </action>
  <verify>
    <automated>pytest tests/switch/test_meraki_client_blocker.py</automated>
  </verify>
  <done>Client blocker switch reflects real-time API state accurately.</done>
</task>

<task type="auto">
  <name>Task 3: Harden SSID Toggles for Reliability</name>
  <files>custom_components/meraki_ha/switch/meraki_ssid_device_switch.py</files>
  <action>
    - Enhance `_update_ssid_setting` with explicit timeout handling.
    - Ensure it verifies the state change after a successful API call.
    - Add more detailed error logging for troubleshooting failed toggles.
  </action>
  <verify>
    <automated>pytest tests/switch/test_meraki_ssid_device_switch.py</automated>
  </verify>
  <done>SSID switches are reliable and self-verifying.</done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID | Category          | Component     | Mitigation Plan                                    |
| --------- | ----------------- | ------------- | -------------------------------------------------- |
| T-04-01   | Tampering         | SSID Switch   | Use session-scoped API calls with token validation |
| T-04-02   | Denial of Service | Client Policy | Apply rate limiting to control commands            |

</threat_model>

<verification>
1. Run `pytest tests/switch/test_meraki_client_blocker.py`
2. Run `pytest tests/switch/test_meraki_ssid_device_switch.py`
</verification>

<success_criteria>

- Client blocking/unblocking works reliably and reflects state.
- SSID toggles successfully update and verify Meraki state.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-4/04-01-SUMMARY.md`
</output>
