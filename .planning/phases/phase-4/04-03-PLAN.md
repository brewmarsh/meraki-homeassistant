---
phase: 04-advanced-control-selective-stats
plan: 03
type: execute
wave: 2
depends_on: ['04-01-PLAN.md']
files_modified:
  - custom_components/meraki_ha/webhook.py
  - custom_components/meraki_ha/binary_sensor/device/camera_motion.py
autonomous: true
requirements:
  - CTRL-06

must_haves:
  truths:
    - "Camera entities reflect real-time 'Person detected' state via webhooks."
  artifacts:
    - path: 'custom_components/meraki_ha/webhook.py'
      provides: 'Webhook alert dispatching for camera events'
    - path: 'custom_components/meraki_ha/binary_sensor/device/camera_motion.py'
      provides: 'Camera motion/person detection sensor'
  key_links:
    - from: 'custom_components/meraki_ha/webhook.py'
      to: 'MerakiCameraMotionSensor'
      via: 'coordinator alert update'
---

<objective>
Integrate MV Camera "Person Detected" events into the webhook handler.
- Extend the webhook handler to support Camera Sense alerts.
- Update camera binary sensors to react to "Person detected" payloads in real-time.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-RESEARCH.md

<interfaces>
From custom_components/meraki_ha/webhook.py:
```python
def _dispatch_webhook_alert(data: dict[str, Any], coordinator: Any) -> None:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate MV Camera "Person Detected" into webhook handler</name>
  <files>custom_components/meraki_ha/webhook.py</files>
  <action>
    - Add a handler function `_handle_person_detected_alert(data: dict, coordinator: Any)` to `webhook.py`.
    - Update `_dispatch_webhook_alert` to recognize the 'Person detected' alert type from Meraki.
    - Ensure it identifies the camera by serial number and updates the coordinator's cached state for that camera.
    - Reference CTRL-06.
  </action>
  <verify>
    <automated>pytest tests/test_webhook_camera_events.py</automated>
  </verify>
  <done>Webhook handler processes 'Person detected' events and notifies coordinators.</done>
</task>

<task type="auto">
  <name>Task 2: Update Camera Motion/Person Detection sensor state</name>
  <files>custom_components/meraki_ha/binary_sensor/device/camera_motion.py</files>
  <action>
    - Update `MerakiCameraMotionSensor` to handle real-time detection updates from the coordinator (triggered by webhooks).
    - Map the detection state to the binary sensor's state.
    - Reference CTRL-06.
  </action>
  <verify>
    <automated>pytest tests/binary_sensor/device/test_camera_motion.py</automated>
  </verify>
  <done>Camera binary sensor accurately reflects person detection state from webhooks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description               |
| ----------------------------- | ------------------------- |
| Meraki Webhook -> Integration | Inbound alerts cross here |

## STRIDE Threat Register

| Threat ID | Category | Component       | Disposition | Mitigation Plan                                                                                              |
| --------- | -------- | --------------- | ----------- | ------------------------------------------------------------------------------------------------------------ |
| T-04-04   | Spoofing | Webhook Handler | mitigate    | Validate `sharedSecret` in every webhook payload (already implemented, verify it's active for camera events) |

</threat_model>

<verification>
1. Run `pytest tests/test_webhook_camera_events.py`
2. Run `pytest tests/binary_sensor/device/test_camera_motion.py`
</verification>

<success_criteria>

- Webhooks correctly process camera analytics alerts.
- Camera sensors update within seconds of a 'Person detected' event.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-4/04-03-SUMMARY.md`
</output>
