---
phase: 04-advanced-control-selective-stats
plan: 03
type: execute
wave: 2
depends_on: ['04-01-PLAN.md']
files_modified:
  - custom_components/meraki_ha/webhook.py
  - custom_components/meraki_ha/binary_sensor/device/camera_motion.py
  - custom_components/meraki_ha/binary_sensor/device/camera_person.py
autonomous: true
requirements:
  - CTRL-06

must_haves:
  truths:
    - "Camera entities reflect real-time 'Person detected' and 'Motion detected' state via webhooks."
  artifacts:
    - path: 'custom_components/meraki_ha/webhook.py'
      provides: 'Webhook alert dispatching for camera events'
    - path: 'custom_components/meraki_ha/binary_sensor/device/camera_motion.py'
      provides: 'Real-time camera motion binary sensor'
    - path: 'custom_components/meraki_ha/binary_sensor/device/camera_person.py'
      provides: 'Real-time camera person detection binary sensor'
  key_links:
    - from: 'custom_components/meraki_ha/webhook.py'
      to: 'MerakiMotionSensor'
      via: 'coordinator alert update'
---

<objective>
Refactor MV Camera sensors to use real-time webhook data instead of polling.
- Convert MerakiMotionSensor to be event-driven via webhooks.
- Add MerakiPersonSensor for real-time person detection alerts.
- Ensure webhook handler correctly updates state and triggers binary sensor updates.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-4/04-RESEARCH.md

<interfaces>
From custom_components/meraki_ha/webhook.py:
```python
def _handle_camera_motion_alert(data: dict, coordinator: Any) -> None:
    ...
def _handle_camera_person_detected_alert(data: dict, coordinator: Any) -> None:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor MerakiMotionSensor for Real-time Webhooks</name>
  <files>custom_components/meraki_ha/binary_sensor/device/camera_motion.py</files>
  <action>
    - Remove `async_update` polling logic from `MerakiMotionSensor`.
    - Update `is_on` to check `device.last_motion_event` within a specific timeframe (e.g. 15-30 seconds).
    - Ensure it reacts to `_handle_coordinator_update`.
  </action>
  <verify>
    <automated>pytest tests/binary_sensor/device/test_camera_motion.py</automated>
  </verify>
  <done>Motion sensor is event-driven and responsive to webhooks.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MerakiPersonSensor for Real-time Detection</name>
  <files>custom_components/meraki_ha/binary_sensor/device/camera_person.py</files>
  <action>
    - Create `MerakiPersonSensor` class.
    - Implement `is_on` to check `device.last_person_detected_event` timeframe.
    - Set appropriate device class (`BinarySensorDeviceClass.MOTION` or custom).
    - Register the entity in the camera platform.
  </action>
  <verify>
    <automated>pytest tests/binary_sensor/device/test_camera_person.py</automated>
  </verify>
  <done>Person detection sensor is functional and event-driven.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Webhook Alert Handling for Cameras</name>
  <files>custom_components/meraki_ha/webhook.py</files>
  <action>
    - Ensure `_handle_camera_motion_alert` and `_handle_camera_person_detected_alert` correctly trigger coordinator refreshes.
    - Verify they correctly identify the target camera and update its internal state.
  </action>
  <verify>
    <automated>pytest tests/test_webhook_camera_events.py</automated>
  </verify>
  <done>Webhook handler reliably dispatches camera alerts to sensors.</done>
</task>

</tasks>

<threat_model>

## STRIDE Threat Register

| Threat ID | Category | Component       | Mitigation Plan                                                                                              |
| --------- | -------- | --------------- | ------------------------------------------------------------------------------------------------------------ |
| T-04-04   | Spoofing | Webhook Handler | Validate `sharedSecret` in every webhook payload (already implemented, verify it's active for camera events) |

</threat_model>

<verification>
1. Run `pytest tests/test_webhook_camera_events.py`
2. Run `pytest tests/binary_sensor/device/test_camera_motion.py`
3. Run `pytest tests/binary_sensor/device/test_camera_person.py`
</verification>

<success_criteria>

- Webhooks correctly process camera analytics alerts.
- Camera binary sensors (Motion and Person) update in real-time.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-4/04-03-SUMMARY.md`
</output>
