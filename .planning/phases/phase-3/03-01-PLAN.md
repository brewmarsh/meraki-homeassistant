---
phase: '03'
plan: '01'
type: 'execute'
wave: 1
depends_on: []
files_modified:
  - 'custom_components/meraki_ha/webhook.py'
  - 'custom_components/meraki_ha/core/api/endpoints/network.py'
  - 'custom_components/meraki_ha/core/api/protocol.py'
autonomous: true
requirements:
  - 'WEB-01'
  - 'WEB-02'
  - 'WEB-04'

must_haves:
  truths:
    - 'The webhook handler responds to GET requests with the Meraki validator string.'
    - 'The webhook handler validates POST payloads using the shared secret.'
    - "Registration automatically enables 'Client connectivity changed' and 'APs went down' alerts in the Meraki Dashboard."
  artifacts:
    - path: 'custom_components/meraki_ha/webhook.py'
      provides: 'Dual-mode (GET/POST) webhook handling'
    - path: 'custom_components/meraki_ha/core/api/endpoints/network.py'
      provides: 'Automated alert and scanning API configuration'
  key_links:
    - from: 'custom_components/meraki_ha/webhook.py'
      to: 'Meraki GET Validator'
      via: 'web.Response'
    - from: 'custom_components/meraki_ha/core/api/endpoints/network.py'
      to: 'Meraki Dashboard API'
      via: 'updateNetworkAlertSettings'
---

<objective>
Enable real-time event reception by implementing a dual-mode webhook handler and automating the registration of required alerts and Scanning API settings in the Meraki Dashboard.

Purpose: Transition from polling-only to real-time updates for presence and hardware health.
Output: Validated webhook infrastructure and automated configuration logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/phase-3/03-RESEARCH.md
@custom_components/meraki_ha/webhook.py
@custom_components/meraki_ha/core/api/endpoints/network.py

<interfaces>
From custom_components/meraki_ha/core/api/protocol.py:
```python
class NetworkEndpointsProtocol(Protocol):
    async def register_webhook(self, webhook_url: str, secret: str, config_entry_id: str) -> list[str]: ...
    # Need to add:
    # async def configure_alerts(self, network_id: str, webhook_id: str) -> None: ...
    # async def configure_scanning_api(self, network_id: str) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Dual-Mode Webhook Handler</name>
  <files>custom_components/meraki_ha/webhook.py</files>
  <behavior>
    - GET request to webhook URL must return the validator string from Meraki.
    - POST request with valid sharedSecret must process the payload.
    - POST request with invalid sharedSecret must return 401/403 or log warning and ignore.
    - Support both Standard Alerts and Scanning API v3 (location) payloads.
  </behavior>
  <action>
    - Update `async_register_webhook` to include `allowed_methods={"GET", "POST"}` in `webhook.async_register`.
    - Modify `async_handle_webhook` to check `request.method`.
    - If GET: Retrieve the validator from the config entry or API and return as plain text (status 200).
    - If POST: Use existing validation logic but ensure it handles Scanning API v3 JSON structure (which may differ slightly from alerts).
    - Refactor dispatch logic to handle 'location' alerts from Scanning API.
  </action>
  <verify>
    <automated>pytest tests/test_webhook.py</automated>
  </verify>
  <done>Webhook handler passes validation challenge and processes signed POST payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Automate Dashboard Alert/Scanning Configuration</name>
  <files>custom_components/meraki_ha/core/api/endpoints/network.py, custom_components/meraki_ha/core/api/protocol.py</files>
  <action>
    - Add `updateNetworkAlertSettings` and `updateNetworkSettings` (for Scanning API) to `NetworkEndpoints`.
    - Update `register_webhook` in `NetworkEndpoints` to call these new methods for each network.
    - Enable "Client connectivity changed" and "APs went down" in `defaultAlertDeliveries` or specific alert settings.
    - Enable Scanning API v3 if not already enabled.
    - Update `NetworkEndpointsProtocol` in `protocol.py` to match new methods.
  </action>
  <verify>
    <automated>pytest tests/api/test_network_endpoints.py</automated>
  </verify>
  <done>Registration logic automatically configures the Meraki Dashboard to send required alerts.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                  | Description                                           |
| ------------------------- | ----------------------------------------------------- |
| Meraki Cloud → HA Webhook | Untrusted internet traffic entering the local network |

## STRIDE Threat Register

| Threat ID | Category               | Component       | Disposition | Mitigation Plan                                                 |
| --------- | ---------------------- | --------------- | ----------- | --------------------------------------------------------------- |
| T-03-01   | Spoofing               | Webhook Handler | mitigate    | Verify `sharedSecret` in every POST payload; use HTTPS only.    |
| T-03-02   | Information Disclosure | Webhook GET     | mitigate    | Only return the validator string; do not expose internal state. |

</threat_model>

<verification>
1. Run `pytest tests/test_webhook.py` to verify validator and secret checks.
2. Verify automated registration logic calls the correct Meraki API endpoints using mocks.
</verification>

<success_criteria>

- Webhook responds to GET with validator.
- Webhook accepts POST with valid secret and fires events.
- Registration logic enables alerts and Scanning API in Meraki Dashboard.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-3/03-01-SUMMARY.md`
</output>
