---
phase: '03'
plan: '02'
type: 'execute'
wave: 2
depends_on: ['03-01']
files_modified:
  - 'custom_components/meraki_ha/coordinators/client.py'
  - 'custom_components/meraki_ha/device_tracker.py'
  - 'custom_components/meraki_ha/const/platform.py'
autonomous: true
requirements:
  - 'WEB-01'
  - 'WEB-03'

must_haves:
  truths:
    - 'New device_tracker entities are only created for non-randomized MACs or existing known clients.'
    - 'Real-time webhook events for client connectivity immediately update device_tracker states.'
    - 'Randomized MAC addresses (locally administered) are correctly detected and filtered.'
  artifacts:
    - path: 'custom_components/meraki_ha/device_tracker.py'
      provides: 'Client presence tracking platform'
    - path: 'custom_components/meraki_ha/coordinators/client.py'
      provides: 'Filtered client data with real-time update support'
  key_links:
    - from: 'custom_components/meraki_ha/webhook.py'
      to: 'custom_components/meraki_ha/coordinators/client.py'
      via: 'Method call or event listener'
    - from: 'custom_components/meraki_ha/coordinators/client.py'
      to: 'custom_components/meraki_ha/device_tracker.py'
      via: 'DataUpdateCoordinator updates'
---

<objective>
Optimize presence detection by filtering out randomized MAC addresses and integrating real-time webhook events into the device_tracker platform.

Purpose: Prevent entity bloat and provide sub-second presence updates.
Output: Active device_tracker platform with real-time capabilities and MAC filtering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/phase-3/03-RESEARCH.md
@custom_components/meraki_ha/coordinators/client.py
@custom_components/meraki_ha/const/platform.py

<interfaces>
From custom_components/meraki_ha/coordinators/client.py:
```python
class MerakiClientCoordinator(MerakiBaseCoordinator):
    async def _async_update_data(self) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MAC Filtering & Known Clients Logic</name>
  <files>custom_components/meraki_ha/coordinators/client.py</files>
  <behavior>
    - `is_randomized_mac("02:00:00:00:00:00")` -> True
    - `is_randomized_mac("01:00:00:00:00:00")` -> False
    - `is_randomized_mac("26:AE:AE:26:AE:AE")` -> True
    - Filter logic: Keep if not randomized OR if already in known clients registry.
  </behavior>
  <action>
    - Implement `is_randomized_mac(mac: str)` utility based on IEEE 802 locally administered address pattern (2nd character is 2, 6, A, or E).
    - Update `MerakiClientCoordinator` to apply this filter during data updates.
    - Ensure that clients seen via polling (Known Clients) are always preserved.
    - Add a "Known Clients" set to the coordinator to track seen MACs across restarts if needed (or rely on registry).
  </action>
  <verify>
    <automated>pytest tests/test_mac_filtering.py</automated>
  </verify>
  <done>Randomized MACs are filtered from the client list unless they are already known.</done>
</task>

<task type="auto">
  <name>Task 2: Device Tracker Integration</name>
  <files>custom_components/meraki_ha/device_tracker.py, custom_components/meraki_ha/const/platform.py, custom_components/meraki_ha/webhook.py</files>
  <action>
    - Add `PLATFORM_DEVICE_TRACKER` to the `PLATFORMS` list in `custom_components/meraki_ha/const/platform.py`.
    - Create `custom_components/meraki_ha/device_tracker.py` using `ScannerEntity`.
    - Implement `async_setup_entry` for `device_tracker` to discover clients from `MerakiClientCoordinator`.
    - Update `webhook.py` to dispatch client connectivity events directly to `MerakiClientCoordinator`.
    - Implement a method in `MerakiClientCoordinator` to handle real-time status updates and notify listeners (triggering `device_tracker` updates).
  </action>
  <verify>
    <automated>pytest tests/test_device_tracker.py</automated>
  </verify>
  <done>Clients are tracked as device_tracker entities and update in real-time via webhooks.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                       | Description                                            |
| ------------------------------ | ------------------------------------------------------ |
| Client Data → HA State Machine | Data from coordinator affecting entity count and state |

## STRIDE Threat Register

| Threat ID | Category          | Component        | Disposition | Mitigation Plan                                                                |
| --------- | ----------------- | ---------------- | ----------- | ------------------------------------------------------------------------------ |
| T-03-03   | Denial of Service | HA State Machine | mitigate    | Implement MAC filtering to prevent entity bloat from randomized passerby MACs. |

</threat_model>

<verification>
1. Run `pytest tests/test_mac_filtering.py` to verify IEEE pattern matching.
2. Run `pytest tests/test_device_tracker.py` to verify entity creation and real-time updates.
</verification>

<success_criteria>

- Only known/non-randomized MACs appear as entities.
- Webhook events trigger immediate state changes in HA.
- Entity registry remains stable without bloat.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-3/03-02-SUMMARY.md`
</output>
