---
phase: 05-final-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/meraki_ha/coordinators/main.py
  - custom_components/meraki_ha/coordinators/base.py
  - custom_components/meraki_ha/coordinators/appliance.py
  - custom_components/meraki_ha/coordinators/camera.py
  - custom_components/meraki_ha/coordinators/client.py
  - custom_components/meraki_ha/coordinators/device.py
  - custom_components/meraki_ha/coordinators/sensor.py
  - custom_components/meraki_ha/coordinators/switch.py
  - custom_components/meraki_ha/coordinators/wireless.py
autonomous: true
requirements: [STB-03]

must_haves:
  truths:
    - 'Coordinators raise UpdateFailed when the API returns an error, preventing entities from showing stale data.'
  artifacts:
    - path: 'custom_components/meraki_ha/coordinators/main.py'
      provides: 'Refined error handling logic'
  key_links:
    - from: 'custom_components/meraki_ha/coordinators/main.py'
      to: 'homeassistant.helpers.update_coordinator.UpdateFailed'
      via: 'Raising exception'
---

<objective>
Refine error handling across all coordinators to use the standard Home Assistant UpdateFailed exception. This ensures that when the Meraki API is unreachable or returns an error, entities correctly indicate they are "Unavailable" instead of showing stale data.

Purpose: Reliability and transparency of integration status.
Output: Coordinators raising UpdateFailed on API errors.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-5/RESEARCH.md
@custom_components/meraki_ha/coordinators/main.py
@custom_components/meraki_ha/coordinators/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update error handling in main coordinator</name>
  <files>custom_components/meraki_ha/coordinators/main.py</files>
  <action>
    Modify the _async_update_data method in custom_components/meraki_ha/coordinators/main.py.
    Instead of returning self.last_successful_data in the catch block (except Exception as err), it should raise UpdateFailed(err).
    Keep the record_failure call for polling management but ensure UpdateFailed is the final result.
    Ensure "429" rate limit handling still specifically informs the user.
  </action>
  <verify>
    <automated>grep -A 10 "except Exception as err:" custom_components/meraki_ha/coordinators/main.py | grep "raise UpdateFailed"</automated>
  </verify>
  <done>Main coordinator raises UpdateFailed on error</done>
</task>

<task type="auto">
  <name>Task 2: Update error handling in sub-coordinators</name>
  <files>
    custom_components/meraki_ha/coordinators/appliance.py,
    custom_components/meraki_ha/coordinators/camera.py,
    custom_components/meraki_ha/coordinators/client.py,
    custom_components/meraki_ha/coordinators/device.py,
    custom_components/meraki_ha/coordinators/sensor.py,
    custom_components/meraki_ha/coordinators/switch.py,
    custom_components/meraki_ha/coordinators/wireless.py
  </files>
  <action>
    Apply the same error handling refinement to all sub-coordinators.
    Check if they have their own _async_update_data implementation or if they rely on a base class.
    If they rely on base.py, update base.py. If they have their own, update each one.
    The goal is to eliminate returning last_successful_data on failure project-wide.
  </action>
  <verify>
    <automated>grep -r "raise UpdateFailed" custom_components/meraki_ha/coordinators/ | wc -l</automated>
  </verify>
  <done>All sub-coordinators raise UpdateFailed on error</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

No new trust boundaries created.

## STRIDE Threat Register

| Threat ID | Category     | Component    | Disposition | Mitigation Plan                                         |
| --------- | ------------ | ------------ | ----------- | ------------------------------------------------------- |
| T-05-02   | Availability | Coordinators | mitigate    | Use UpdateFailed to correctly signal state to the user. |

</threat_model>

<verification>
Run `pytest tests/test_coordinator.py` and ensure it still passes (or update it to expect UpdateFailed).
</verification>

<success_criteria>

- Terminal API errors lead to UpdateFailed.
- Entities show as unavailable during outages.
  </success_criteria>

<output>
After completion, create `.planning/phases/phase-5/05-02-SUMMARY.md`
</output>
