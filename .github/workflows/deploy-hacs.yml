name: Deploy to HACS

on:
  pull_request:
    types: [closed]
    branches:
      - main
      - beta

jobs:
  version:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    permissions:
      contents: write
      pull-requests: read
    outputs:
      new_version: ${{ steps.increment.outputs.new_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
<<<<<<< HEAD
          ref: beta

      - name: Set up Python and Node.js
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - uses: actions/setup-node@v4
=======
          ref: ${{ github.base_ref }}

      - name: Set up Python and Node.js
        uses: actions/setup-python@v6
        with:
          python-version: '3.x'
      - uses: actions/setup-node@v6
>>>>>>> origin/beta
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install bump-my-version
          npm install

      - name: Increment Version
        id: increment
<<<<<<< HEAD
        run: |
          bump-my-version bump patch --allow-dirty
=======
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          if [[ "${{ github.base_ref }}" == "beta" ]]; then
            bump-my-version bump beta --allow-dirty
          else
            increment_type="patch" # Default increment type
            if [[ "$PR_TITLE" == *"[major]"* ]]; then
              increment_type="major"
            elif [[ "$PR_TITLE" == *"[minor]"* ]]; then
              increment_type="minor"
            fi
            bump-my-version bump $increment_type --allow-dirty
          fi
>>>>>>> origin/beta
          new_version=$(grep -oP '"version": "\K[^"]+' custom_components/meraki_ha/manifest.json)
          echo "new_version=$new_version" >> "$GITHUB_OUTPUT"

      - name: Update package.json version
        if: steps.increment.outputs.new_version != ''
        run: |
          echo "Synchronizing package.json version to: ${{ steps.increment.outputs.new_version }}"
          npm version ${{ steps.increment.outputs.new_version }} --no-git-tag-version --allow-same-version

      - name: Commit and push changes
        if: steps.increment.outputs.new_version != ''
<<<<<<< HEAD
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore(release): update version to ${{ steps.increment.outputs.new_version }}'
          file_pattern: 'custom_components/meraki_ha/manifest.json package.json .bumpversion.cfg package-lock.json'
          commit_user_name: 'github-actions[bot]'
          commit_user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_author: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          branch: beta
=======
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'chore(release): update version to ${{ steps.increment.outputs.new_version }}'
          file_pattern: 'custom_components/meraki_ha/manifest.json package.json pyproject.toml package-lock.json'
          commit_user_name: 'github-actions[bot]'
          commit_user_email: 'github-actions[bot]@users.noreply.github.com'
          commit_author: 'github-actions[bot] <github-actions[bot]@users.noreply.github.com>'
          branch: ${{ github.base_ref }}
>>>>>>> origin/beta

  release:
    runs-on: ubuntu-latest
    needs: version
    if: needs.version.outputs.new_version != ''
    permissions:
      contents: write
    steps:
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: 'v${{ needs.version.outputs.new_version }}'
          name: 'Release v${{ needs.version.outputs.new_version }}'
          generate_release_notes: true
          draft: false
          prerelease: ${{ github.base_ref == 'beta' }}
