name: Pull Request Validation

on:
  pull_request:
    branches:
      - main
      - beta

permissions:
  contents: write
  pull-requests: write
  checks: write

jobs:
  quality-gate:
    uses: ./.github/workflows/reusable-quality-checks.yaml
    with:
      python-version: '3.13'
    secrets: inherit
