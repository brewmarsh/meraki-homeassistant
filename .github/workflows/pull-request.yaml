name: Pull Request Validation

on:
  pull_request:
    branches:
      - main
      - beta

jobs:
  validate:
    uses: ./.github/workflows/reusable-quality-checks.yaml
    with:
      python-version: '3.13'
    secrets: inherit
